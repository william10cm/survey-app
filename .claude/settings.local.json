{
  "permissions": {
    "allow": [
      "PowerShell(aws cloudformation describe-stacks --stack-name survey-app --region us-east-1 --query \"Stacks[0].StackStatus\" --output text)",
      "PowerShell(git add .github/workflows/deploy.yml)",
      "PowerShell(git push origin main)"
    ]
  }
}
